<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <%- include('./component/head', {title: 'Home'}) %>
</head>

<body>
  <%- include('./component/header')%>
  <h1>Hello EJS</h1>
  <% for (let i=0; i < 5; i ++) { %>
  <div>hi~ <%= i %>번째</div>
  <%= i %>
  <% } %>

  <!-- btns 버튼 -->
  <% console.log(btns) %>
  <% for(let btn of btns) { %>
  <button type="button" class="btn btn-info"><%= btn %></button>
  <% } %>



</body>

</html>